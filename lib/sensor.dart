import 'package:flutter/material.dart';

class ourSensor extends StatelessWidget {
  const ourSensor({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
